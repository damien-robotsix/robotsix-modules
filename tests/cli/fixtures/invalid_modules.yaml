modules:
  - description: This module is missing its required id field.
    paths:
      - src/no_id/**
  - description: Another module also missing its required id field.
    paths:
      - src/also_bad/**
