# Gitleaks configuration for robotsix-modules.
# Docs: https://github.com/gitleaks/gitleaks?tab=readme-ov-file#configuration

useDefault = true

[allowlist]
  description = "Test fixtures and lockfiles"

  paths = [
    '''uv\.lock''',
    '''tests/.*/fixtures/.*''',
    '''\.git/.*''',
  ]
