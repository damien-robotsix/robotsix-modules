# Per-repo periodic workflow: the PRESENCE of this file enables the
# `bc_check` periodic for robotsix-modules. It scans the repo for
# backward-compatibility shims/aliases/no-op compat code ripe for
# removal and files draft tickets. Schedule (interval: 1d), model,
# tool list, and system prompt are all inherited from the mill's
# built-in agent_definitions/periodic/bc_check.yaml via partial-merge
# — this file only needs `name`.
name: bc_check
