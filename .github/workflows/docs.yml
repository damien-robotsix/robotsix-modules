name: Docs

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  docs:
    uses: damien-robotsix/robotsix-mill/.github/workflows/python-docs.yml@main
    secrets: inherit
